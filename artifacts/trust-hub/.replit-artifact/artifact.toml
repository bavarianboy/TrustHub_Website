kind = "web"
previewPath = "/"
title = "Trust Hub Business Solutions"
version = "1.0.0"
id = "artifacts/trust-hub"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25227

[services.development]
run = "pnpm --filter @workspace/trust-hub run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/trust-hub", "run", "build" ]
publicDir = "artifacts/trust-hub/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25227"
BASE_PATH = "/"
