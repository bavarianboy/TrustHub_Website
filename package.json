{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node ./scripts/preinstall.mjs",
    "dev": "concurrently --names api,web --prefix-colors magenta,cyan \"pnpm run dev:api\" \"pnpm run dev:web\"",
    "dev:api": "pnpm --filter @workspace/api-server run dev",
    "dev:web": "pnpm --filter @workspace/trust-hub run dev",
    "db:push": "pnpm --filter @workspace/db run push",
    "codegen": "pnpm --filter @workspace/api-spec run codegen",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --if-present run typecheck"
  },
  "private": true,
  "packageManager": "pnpm@10.34.5",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  }
}
